dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        mavenCentral()
    }
}

rootProject.name = "KayJam"

include(':cli')
include(':core')
include(':backend:common')
include(':backend:ts')
include(':backend:jvm')

